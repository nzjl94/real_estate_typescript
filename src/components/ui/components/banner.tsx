import React    from 'react'
import styled   from "styled-components";


const Container = styled.div`${({theme}) => ``}`;

const banner = () => {
    return <Container className='bg-gray-1'>
        banner
    </Container>
}

export default banner